cat << EOF
# nginx
nginx:
  image: nginx:1.11.3
  env_file:
    - ./.env
  dns: ${DNS_ADDRESS}
  links: [ ${POCCI_PUBLIC_CONTAINERS} ]
  volumes:
   - ./nginx:/etc/nginx/conf.d
  ports:
    - "80:80"
    - "443:443"

EOF
