#!/bin/bash
set -e

BASE_DIR=$(cd $(dirname $0)/..; pwd)
source ${BASE_DIR}/bin/lib/init-env

${LIB_DIR}/change-setup-file "$@"
${BIN_DIR}/up-service
${LIB_DIR}/update-jenkins-plugins
${LIB_DIR}/setup
${LIB_DIR}/start-jenkins-slave-nodes
echo "Done."
