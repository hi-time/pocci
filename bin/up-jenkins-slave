#!/bin/bash
set -e

BASE_DIR=$(cd $(dirname $0)/..; pwd)
source ${BASE_DIR}/bin/lib/init-env
source ${LIB_DIR}/force-recreate
${LIB_DIR}/docker-compose jenkins-slaves.yml -p poccin up -d ${FORCE_RECREATE}
