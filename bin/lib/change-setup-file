#!/bin/bash
set -e

SETUP_FILE=setup.default.yml

if [ -n "$1" ]; then
    SETUP_FILE=setup.$1.yml
fi

BASE_DIR=$(cd $(dirname $0)/../..; pwd)
source ${BASE_DIR}/bin/lib/init-env

echo "${SETUP_FILE} --> setup.yml"
cp ${CONFIG_DIR}/${SETUP_FILE} ${CONFIG_DIR}/setup.yml
